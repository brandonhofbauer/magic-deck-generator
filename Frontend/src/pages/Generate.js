import React from 'react';

export const Generate = () => {
    return (
        <>
           <textarea id="output" name="output" rows="10" cols="50">this is the box where the card output will be generated.</textarea> 
        </>
    );
}

export default Generate;